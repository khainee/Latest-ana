name: ci

on:
  push:
    branches: master

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      -
        name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: docker.io
          username: khainezayye
          password: Hp206164$
      -
        name: Build and push
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: khainee/kzmodify
